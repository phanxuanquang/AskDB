<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AskDB.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AskDB.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid" Padding="20">
        <Border
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        BorderThickness="0"
        Background="{StaticResource LayerOnMicaBaseAltFillColorDefault}"
        CornerRadius="15"
        Padding="40"
        MinWidth="800">

            <StackPanel Orientation="Vertical" >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBox x:Name="queryBox" TextWrapping="Wrap" IsSpellCheckEnabled="True" PlaceholderText="Enter your question . . ." AutomationProperties.Name="simple text editor" MinWidth="1000" Margin="0,0,10,0"/>
                    <Button x:Name="myButton" Click="SendButton_Click" Style="{StaticResource AccentButtonStyle}" Width="60" Height="35">
                        <SymbolIcon Symbol="Send"/>
                    </Button>
                </StackPanel>
                <RichEditBox x:Name="resultBox" AutomationProperties.Name="simple text editor" IsReadOnly="True"></RichEditBox>
                <controls:DataGrid x:Name="myDataGrid" MinHeight="150" Margin="0,20,0,10" IsReadOnly="True" AutoGenerateColumns="False" VerticalAlignment="Top"/>

            </StackPanel>

        </Border>
    </Grid>
</Page>
