<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AskDB.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AskDB.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid" Padding="100">
        <Border
        HorizontalAlignment="Stretch"
        VerticalAlignment="Center"
        BorderThickness="0"
        Background="{StaticResource LayerOnMicaBaseAltFillColorDefault}"
        CornerRadius="15"
        Padding="40"
        MinWidth="800">

            <StackPanel Orientation="Vertical" >
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="backButton" Grid.Column="0" BorderThickness="0" Background="Transparent">
                        <SymbolIcon Symbol="Back"/>
                    </Button>
                    <TextBlock Text="ASK ME EVERYTHING ABOUT YOUR DATABASE" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1"/>
                    <Button x:Name="forwardButton" Grid.Column="2" BorderThickness="0" Background="Transparent" IsEnabled="False">
                        <SymbolIcon Symbol="Forward"/>
                    </Button>
                </Grid>
                <Expander x:Name="selectTableExpander" MinWidth="1070" Margin="0,0,0,20" IsExpanded="False" HorizontalAlignment="Stretch">
                    <Expander.Header>
                        <ContentControl FontWeight="SemiBold">Select tables to ask</ContentControl>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel>
                            <ListView 
                                x:Name="tablesListView" 
                                SelectionMode="Multiple" 
                                BorderThickness="2"
                                BorderBrush="{ThemeResource LayerOnMicaBaseAltFillColorTransparent}"
                                UseLayoutRounding="True"
                                Width="800"
                                MaxHeight="400"
                                HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Expander.Content>
                </Expander>

                <StackPanel x:Name="LoadingOverlay" Background="Transparent" Visibility="Collapsed" Margin="0,0,0,20">
                    <ProgressRing IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel x:Name="mainPanel">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <TextBox x:Name="queryBox" HorizontalAlignment="Stretch" TextWrapping="Wrap" IsSpellCheckEnabled="False" PlaceholderText="Enter your question . . ." AutomationProperties.Name="simple text editor" MinWidth="1000" Margin="0,0,10,0"/>
                        <Button x:Name="myButton" Click="SendButton_Click" Style="{StaticResource AccentButtonStyle}" Width="60" Height="35" Margin="0,0,10,0">
                            <SymbolIcon Symbol="Send"/>
                        </Button>
                        <Button x:Name="exportButton" Style="{StaticResource DefaultButtonStyle}" Width="60" Height="35" Visibility="Collapsed" Margin="0,0,10,0" >
                            <SymbolIcon Symbol="Download"/>
                            <Button.Resources>
                                <TeachingTip x:Name="DownloadTip"
                                    Target="{x:Bind exportButton}"
                                    Title="Export"
                                    Subtitle="Save the output data as an Excel file.">
                                </TeachingTip>
                            </Button.Resources>
                        </Button>
                        <Button x:Name="showSqlButton" Style="{StaticResource DefaultButtonStyle}" Width="60" Height="35" Visibility="Collapsed">
                            <SymbolIcon Symbol="View"/>
                            <Button.Resources>
                                <TeachingTip x:Name="ShowSqlTip"
                                    Target="{x:Bind showSqlButton}"
                                    Title="Show SQL command"
                                    Subtitle="Reveal the SQL query generated by the AI.">
                                </TeachingTip>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                    <controls:DataGrid x:Name="outputGridView" MaxHeight="800" IsReadOnly="True" AutoGenerateColumns="False" VerticalAlignment="Top" Margin="0,0,0,20"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
